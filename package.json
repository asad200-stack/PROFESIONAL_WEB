{
  "name": "multi-store-saas",
  "version": "1.0.0",
  "description": "Multi-Store SaaS: simple storefronts with WhatsApp ordering, themes, and admin dashboard (Express + Prisma + Vanilla JS).",
  "main": "src/server.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node scripts/prisma-setup.js && node src/server.js",
    "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:migrate": "prisma migrate deploy",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^5.16.1"
  },
  "engines": {
    "node": ">=18"
  }
}


