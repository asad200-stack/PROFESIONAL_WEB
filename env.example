PORT=3000
# PostgreSQL example (Railway will provide DATABASE_URL automatically if you add a Postgres service)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public

# JWT secret for admin auth
JWT_SECRET=super_secret_jwt_key_change_me

# Optional base URL to generate absolute product links in WhatsApp messages
# If not set, server will derive from the request origin
BASE_URL=https://your-domain.com


